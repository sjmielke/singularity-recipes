Bootstrap: shub
From: marcc-hpc/pytorch

%post
  # Downgrade pytorch
  /opt/conda/bin/pip install torch==0.4.1
  # Reinstall most current tensorbaordX
  /opt/conda/bin/conda install -f -y -c conda-forge tensorboardx
